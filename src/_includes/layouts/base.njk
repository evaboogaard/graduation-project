<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    {% include "partials/meta-info.njk" %}

    <script>
      document
        .documentElement
        .classList
        .remove("no-js");
      document
        .documentElement
        .classList
        .add("js-active");
    </script>

    {% if env.nodeenv !== 'production' %}
      <!-- We're not in production mode -->
      <link rel="stylesheet" href="/assets/css/global.css" />
    {% endif %}

    <link href="https://fonts.googleapis.com/css2?family=Roboto&family=Open+Sans&family=Lora&family=Poppins&family=Slabo+27px&family=Montserrat&family=Merriweather&family=Playfair+Display&display=swap" rel="stylesheet">
  </head>

  <body>
    {# {% include "partials/site-head.njk" %} #}

    <main tabindex="-1" id="main-content" class="leading-snug">
      {% block content %}
        {{ content | safe }}
      {% endblock %}
    </main>

    {# {% include "partials/site-foot.njk" %} #}

    {% script src = "global.js" %}
  </body>
</html>